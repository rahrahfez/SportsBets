using Entities.Models;

namespace Entities.Extensions
{
    public static class WagerExtensions
    {
        public static void Map(this Wager dbWager, Wager wager)
        {
            // dbWager.Id = wager.Id;
            // dbWager.CreatedById = wager.CreatedById;
            // dbWager.AcceptedById = wager.AcceptedById;
            // dbWager.CreatedAt = wager.CreatedAt;
            // dbWager.WagerAmount = wager.WagerAmount;
        }
    }
}